<div class="jumbotron">
  <div class="container">
	<% unless current_user %>
	  <h1>Bienvenue !</h1>
	<% else %>
	  <h1 class="display-3">Bienvenue <%= current_user.first_name %> !</h1>
	<% end %>      
    <p>Eventbrite rapproche les gens grâce à des expériences en direct. Découvrez des événements qui correspondent à vos passions ou créez votre propre événement...</p>
    <p><a class="btn btn-primary btn-lg" href="#" role="button">Learn more »</a></p>
  </div>
</div>

<div class="container text-center">
  <div class="row">
	<% i = 0
	@events.each do |event| %>
	<div class="col-md-4 t-4 text-center">
	  <h2><%=@events[i].title%></h2>
	  <p><em><%=@events[i].description%></em></p>
	  <p>Le <%=@events[i].start_date%></p>
	  <p>A <%=@events[i].location%></p>
	  <p>Pendant <%=@events[i].duration%> heure(s)</p>
	  <p><a class="btn btn-secondary" href="<%= event_path(@events[i].id)%>" role="button">View details »</a></p>
	</div>
	<% i += 1 
	end %>
  </div>
</div>